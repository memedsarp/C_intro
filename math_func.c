#include <stdio.h>
#include <math.h>

int main(int argc, char const *argv[])
{
    double x = sqrt(9);
    double y = pow(3, 4);
    int z = round(3.14);
    int a = ceil(3.14);
    int b = floor(3.14);
    double c = fabs(-15);
    double d = log(3);



    return 0;
}
